{
  "scripts": {
    "serve": "webpack-dev-server",
    "build": "cargo check && wasm-pack build",
    "deploy": "rimraf docs/*.wasm ; webpack --build"
  },
  "devDependencies": {
    "html-webpack-plugin": "^3.2.0",
    "rimraf": "^3.0.2",
    "webpack": "^4.42.0",
    "webpack-cli": "^3.3.11",
    "webpack-dev-server": "^3.10.3"
  }
}
